# Validação estrita (apenas números, 7 ou 8 dígitos). 
# Se não usarmos âncoras (^ e $), o regex \d{7,8} daria "match" dentro de um texto maior 
# (ex: "abc12345678xyz").
# Precisamos travar o início e fim.

^\d{7,8}$